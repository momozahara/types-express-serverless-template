datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model Setting {
  id    String @id @default(uuid())
  name  String
  value String
}
